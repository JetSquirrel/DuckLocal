# Rust build artifacts
/target

# macOS packaging output (scripts/package-macos.sh writes these into the
# repository root; CI uploads them as release assets)
/DuckLocal.app/
/dmg-root/
/ducklocal-macos-*.dmg

# macOS
.DS_Store

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo

# Local secrets (defensive; the app does not read env files today)
.env
.env.*
*.local

# Publish only the official DuckLocal skill; keep local development skills private.
/skills/*
!/skills/ducklocal/
